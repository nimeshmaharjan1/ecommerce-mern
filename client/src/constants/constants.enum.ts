export enum storeStatus {
  IDLE = "idle",
  LOADING = "loading",
  SUCCEEDED = "succeeded",
  FAILED = "failed",
}
export enum Category {
  LAPTOP = "laptop",
  CAMERA = "camera",
  MOBILE = "Mobile",
  SHIRT = "shirt",
  PANT = "pant",
}
